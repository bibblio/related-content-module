{
  "name": "bibblio-related-content-module",
  "version": "1.0.3",
  "description": "The following snippet shows related content module initialisation. See `example.html` for a full, working demo that uses querystring arguments for access token and ids.",
  "main": "example.js",
  "scripts": {
    "test": "NODE_PATH=. istanbul cover _mocha -- -R spec test/unit/* && NODE_PATH=. nightwatch -c test/integration/nightwatch.json test/integration/tests.js",
    "test-unit": "NODE_PATH=. istanbul cover _mocha -- -R spec test/unit/*",
    "test-integration": "NODE_PATH=. nightwatch -c test/integration/nightwatch.json test/integration/tests.js",
    "build": "NODE_PATH=. node build/build.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bibblio/related-content-module.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/bibblio/related-content-module/issues"
  },
  "homepage": "https://github.com/bibblio/related-content-module#readme",
  "dependencies": {
    "xmlhttprequest": "1.8.0"
  }
}